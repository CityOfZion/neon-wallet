import transactionReducer from '../../app/modules/transactions'

describe('transactions module tests', () => {
  const initialState = {
    isLoadingTransactions: false
  }
  test('add tests to me...', () => {
    expect(true).toEqual(true)
  })
})
